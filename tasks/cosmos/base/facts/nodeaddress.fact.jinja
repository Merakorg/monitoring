#!/bin/bash
echo "{\"nodeaddress\" : \"$({% if passphrase != False %}echo {{passphrase}} | {% endif %} {{ chaincommand }} keys show {{ from }} {% if backend != False %}--keyring-backend {{ backend }}{% endif %} {{ extrabin }} |grep address|cut -d ":" -f 2-|xargs echo)\"}"
